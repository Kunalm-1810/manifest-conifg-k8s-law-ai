apiVersion: apps/v1
kind: Deployment
metadata:
  name: be-deployment
  labels:
    app: be-law-ai
    env: staging
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
  selector:
    matchLabels:
      app: be-law-ai
      env: staging
  template:
    metadata:
      labels:
        app: be-law-ai
        env: staging
    spec:
      containers:
      - name: be-law-ai-container
        image: kunalmane/be-law-ai-project:v1.1
        ports:
        - containerPort: 5000
        envFrom:
          - secretRef:
              name: law-ai-secret
        resources:
          limits:
            memory: "512Mi"
            cpu: "500m"
          requests:
            memory: "256Mi"
            cpu: "250m"
