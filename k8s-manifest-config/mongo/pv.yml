apiVersion: v1
kind: PersistentVolume
metadata:
  name: mongo-pv
  labels:
    app: mongo
    env: staging
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: mongo-storage-class
  hostPath: 
    path: /data/mongo
    type: DirectoryOrCreate