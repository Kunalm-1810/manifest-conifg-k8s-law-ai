apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongo-pvc
  labels:
    app: mongo
    env: staging
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: mongo-storage-class
  resources:
    requests:
      storage: 5Gi